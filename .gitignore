# Source material, kept out of git.
#
#   _source/photos   originals the committed crops were made from
#   _source/logo-refs  the CVVI and Gemini logo explorations, plus the unused
#                      stock wave banner
#   _source/records    personal Monash Pure exports for named individuals —
#                      these must not travel with the repo
#
# Nothing here is needed to build or serve the site. Delete the folder if you
# have the originals elsewhere; the crops in the repo are independent of it.
_source/

# Scratch previews
_*.html

.DS_Store
